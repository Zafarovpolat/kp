# 🚀 Деплой KP Generator

Этот гайд покажет как задеплоить ваше приложение **БЕСПЛАТНО** на Vercel.

---

## 📋 Что нужно знать

**Нужен ли `proxy.py` на продакшене?**  
✅ **ДА**, но в виде Vercel Serverless Function, а не Flask сервера.

**Что изменилось:**
- Локальный Flask сервер (`proxy.py` в корне) используется только для разработки
- На Vercel используется serverless function (`api/proxy.py`)
- `script.js` автоматически определяет окружение

---

## 🎯 Вариант 1: Деплой на Vercel (Рекомендовано)

### Преимущества:
- ✅ Полностью бесплатно
- ✅ Автоматический SSL (HTTPS)
- ✅ CDN по всему миру
- ✅ Serverless функции включены
- ✅ Автоматические деплои из GitHub

### Шаги:

#### 1. Подготовка проекта

Убедитесь, что в папке `c:\Users\x\Downloads\kp` есть:
```
kp/
├── app/                  # Фронтенд
│   ├── index.html
│   ├── generator.html
│   ├── script.js
│   └── styles.css
├── api/                  # Serverless функция
│   └── proxy.py
├── vercel.json          # Конфигурация Vercel
└── .gitignore
```

✅ Все эти файлы уже созданы!

#### 2. Установка Vercel CLI (опционально)

Если хотите деплоить из командной строки:

```powershell
npm install -g vercel
```

#### 3. Деплой через Vercel Dashboard (простой способ)

1. **Зарегистрируйтесь на Vercel:**
   - Перейдите на [vercel.com](https://vercel.com)
   - Нажмите "Sign Up" → выберите "Continue with GitHub"

2. **Создайте GitHub репозиторий:**
   ```powershell
   cd c:\Users\x\Downloads\kp
   git init
   git add .
   git commit -m "Initial commit"
   ```
   
   Затем создайте репозиторий на GitHub и запушьте:
   ```powershell
   git remote add origin https://github.com/YOUR_USERNAME/kp-generator.git
   git push -u origin main
   ```

3. **Импортируйте проект в Vercel:**
   - В Vercel Dashboard нажмите "Add New Project"
   - Выберите ваш GitHub репозиторий
   - **Root Directory**: оставьте пустым
   - **Build Settings**: 
     - Framework Preset: "Other"
     - Output Directory: `app`
   - Нажмите "Deploy"

4. **Готово!** Vercel автоматически:
   - Задеплоит ваши файлы
   - Настроит serverless function из `api/proxy.py`
   - Выдаст вам URL типа `https://kp-generator-xxxxx.vercel.app`

#### 4. Деплой через Vercel CLI (альтернатива)

```powershell
cd c:\Users\x\Downloads\kp
vercel login
vercel --prod
```

Vercel задаст вопросы:
- "Set up and deploy?" → `Y`
- "Which scope?" → выберите ваш аккаунт
- "Link to existing project?" → `N`
- "What's your project's name?" → `kp-generator`
- "In which directory is your code located?" → `.`
- "Want to override the settings?" → `N`

---

## 🔧 Вариант 2: Другие платформы

### Netlify
Похож на Vercel, но serverless functions настраиваются немного иначе.

### Render.com
Можно задеплоить как статику + отдельный Python web service для `proxy.py`.

---

## ⚙️ Для локальной разработки

### Запуск приложения локально:

1. **Запустите Flask прокси:**
   ```powershell
   cd c:\Users\x\Downloads\kp
   python proxy.py
   ```
   Прокси запустится на `http://localhost:8080`

2. **Откройте приложение:**
   - Используйте Live Server в VS Code
   - Или просто откройте `app/index.html` в браузере

3. **Всё готово!** AI будет работать через локальный прокси.

---

## 🔐 Безопасность API ключа

⚠️ **ВАЖНО:** Сейчас API ключ Zenmux хранится в `script.js` (hardcoded).

**Для production:**
1. Создайте файл `.env` в корне проекта:
   ```
   ZENMUX_API_KEY=sk-ai-v1-2cf03b8d30e1b2672de17174830929e852b057b8609b972a8faa4ae871dab488
   ```

2. В Vercel Dashboard → Settings → Environment Variables:
   - Добавьте `ZENMUX_API_KEY` с вашим ключом

3. Измените `api/proxy.py` чтобы читать ключ из environment:
   ```python
   import os
   api_key = os.environ.get('ZENMUX_API_KEY', '')
   ```

4. Уберите hardcoded ключ из `script.js` - пусть фронтенд просто отправляет запросы в `/api/chat`, а serverless function сама подставляет ключ.

---

## 📝 Troubleshooting

### Проблема: "API Error: 500"
- Проверьте логи в Vercel Dashboard → Deployments → Functions
- Убедитесь что API ключ валидный

### Проблема: "CORS error" на production
- Убедитесь что `vercel.json` правильно настроен
- Проверьте что serverless function возвращает CORS headers

### Проблема: Локально не работает
- Убедитесь что `proxy.py` запущен
- Проверьте что в `script.js` используется `localhost:8080`

---

## 🎉 Готовые команды для быстрого деплоя

```powershell
# 1. Инициализация Git (если ещё не сделано)
cd c:\Users\x\Downloads\kp
git init
git add .
git commit -m "Initial commit: KP Generator with AI"

# 2. Создайте репозиторий на GitHub, затем:
git remote add origin https://github.com/YOUR_USERNAME/kp-generator.git
git branch -M main
git push -u origin main

# 3. Деплой на Vercel
npm install -g vercel   # Если ещё не установлено
vercel login
vercel --prod
```

**Или просто используйте Vercel Dashboard** (ещё проще!):
1. vercel.com → Import Project
2. Выберите ваш GitHub репозиторий
3. Deploy!

---

## 📧 Нужна помощь?

Если возникнут проблемы при деплое, проверьте:
- Логи в Vercel Dashboard
- Browser console (F12)
- Network tab для CORS ошибок

Удачного деплоя! 🚀
